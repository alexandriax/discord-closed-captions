# Required. Keep this server-side; never copy it into the extension.
OPENAI_API_KEY=

# Local relay settings.
HOST=127.0.0.1
PORT=8787

# Optional shared gate for early remote-relay testing.
DISCCORD_ACCESS_KEY=

# Live transcription model.
OPENAI_TRANSCRIPTION_MODEL=gpt-live-transcribe
